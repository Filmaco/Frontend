import { IToast, TToastOptions } from "./types";
type ToastHandler = (message: string, options?: TToastOptions) => string;
declare const toasts: import("vue").Ref<IToast[]>;
declare const toast: {
    (message: string, options?: TToastOptions): void;
    error: ToastHandler;
    success: ToastHandler;
    info: ToastHandler;
    warning: ToastHandler;
    remove(id: string): void;
    dismissAll(): void;
};
export { toast, toasts };
