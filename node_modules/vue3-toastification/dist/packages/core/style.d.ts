export default class Style {
    private type;
    private variant;
    constructor(type?: string, variant?: string);
    get(): {
        container: string;
        icon: string;
        title: string;
        content: string;
        button: string;
        progressBar: string;
    };
}
