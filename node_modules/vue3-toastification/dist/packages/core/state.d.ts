import { Ref } from 'vue';
import { IToast } from './types';
export default function useToasts(): {
    toasts: Ref<IToast[]>;
    add: (options: IToast) => string | number;
    remove: (id: string) => void;
};
